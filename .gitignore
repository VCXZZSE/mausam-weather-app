node_modules/
dist/
.dist/
build/
*.zip
.cache/
.env*
!.env.example
# Committed on purpose: the Android build configuration. Contains only
# public HTTPS endpoints, never a secret. See .env.android for why.
!.env.android
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
.vercel

# Local-only backend handoff doc — never commit
/docs/BACKEND_HANDOFF_LOCAL.md
